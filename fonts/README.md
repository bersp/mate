# Project fonts

Font files in this directory are on the Typst font path for both measurement
and rendering. A non-bundled font used by a template or deck must live here or
in a `config.font_paths` directory. Typst's embedded faces (Libertinus Serif,
New Computer Modern, ...) are available without any file here.
